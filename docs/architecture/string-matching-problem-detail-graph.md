# 模式匹配问题第三层图谱说明

最后更新：2026-03-19

## 1. 目标

本文件用于说明“`string-matching-problem` / 模式匹配问题”这一第三层节点的来源、拆分依据与依赖关系设计。

本次目标是把“串”二级图谱中的“模式匹配问题”继续拆到“主串、模式串、定位目标、成功失败判定与应用场景”这一层，让简单匹配和 KMP 前面的概念入口更完整。

## 2. 材料来源

- `docs/Table_of_Contents_for_Data_Structures.pdf`

直接依据：

- 第四章 4.3 串的模式匹配
- 主串与模式串
- 定位问题
- 成功失败判定

## 3. 第三层节点

当前拆成以下 8 个节点：

1. 主串
2. 模式串
3. 定位目标
4. 逐字符比较思想
5. 匹配成功与失败
6. 首个出现位置
7. 应用场景
8. 应用入口

## 4. 设计价值

这一层适合答辩时说明：

- 为什么模式匹配问题本身就值得单独成层，而不是直接进入某个算法
- 为什么“主串 / 模式串 / 位置结果”是后续所有匹配算法共享的公共语义
- 为什么这一层能让“简单匹配”和“KMP”之间的关系更清楚

## 5. 当前边界

本次只新增展示层第三层图谱，不改变路径规划算法和数据库结构。
